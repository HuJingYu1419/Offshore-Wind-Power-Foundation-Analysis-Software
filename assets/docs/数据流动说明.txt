用户点击公式卡片
    ↓
main.js (selectModule)
    ↓
formGenerator.js (生成参数表单)
    ↓
用户输入参数 + 点击计算
    ↓
模块.calculate(params) → 返回结果对象
    ↓
resultRenderer.js (主入口)
    ↓
renderers/index.js (查找专用渲染器)
    ↓
专用渲染器 (如 torpedoAnchorRenderer.js)
    ↓
baseRenderer.js (调用通用组件)
    ↓
渲染结果 → DOM 更新



